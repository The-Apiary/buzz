p
  if results.length
    | Not what you're looking for?
  else
    | No Search Results found.
  / Whitespace between above sentences and link
  |  
  = link-to 'podcast.new' query | Add a podcast.

each results
  link-to 'podcast.show' id
    .podcast
      .pull-left
        img src=image_url

      h5.media-heading
        | {{title}}

